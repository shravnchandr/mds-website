[project]
name = "mds-website"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Shravan Chandra", email = "shravnchandr@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "jupyter>=1.1.1",
    "matplotlib>=3.11.2",
    "pandas>=3.0.6",
    "seaborn>=0.13.2",
]

[project.scripts]
mds-website = "mds_website:main"

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"
